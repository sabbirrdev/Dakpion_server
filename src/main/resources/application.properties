# Active Profile: Defaults to 'local', can be overridden with SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/Khuda-Lagce}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:2003}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never

app.jwt-secret=${APP_JWT_SECRET:3fca397df901b8cd7a486b62c4d43b14d397318a8bfce625718712fc48b71cd3}
app-jwt-expiration-milliseconds=${APP_JWT_EXPIRATION:7200000}


################################################################################
# Resources DIR
resources.dir = /resources/KhudaLagce/
#resources.dir = src/main/resources/
################################################################################


################################################################################
## resources Upload and Download filter
image.upload.dir = ${resources.dir}images/
file.upload.dir = ${resources.dir}files/
video.upload.dir = ${resources.dir}video/


# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max request size.
spring.servlet.multipart.max-request-size=200MB


################################################################################
## SSL COMMERZ INRREGATION
sslcommerz.store-id=${SSLCOMMERZ_STORE_ID:msrte686a99e1b76ab}
sslcommerz.store-password=${SSLCOMMERZ_STORE_PASSWORD:msrte686a99e1b76ab@ssl}
sslcommerz.init-url=${SSLCOMMERZ_INIT_URL:https://sandbox.sslcommerz.com/gwprocess/v4/api.php}
sslcommerz.success-url=${SSLCOMMERZ_SUCCESS_URL:https://ekhoni-server.onrender.com/api/private/payment/success}
sslcommerz.fail-url=${SSLCOMMERZ_FAIL_URL:https://ekhoni-server.onrender.com/api/private/payment/fail}
sslcommerz.cancel-url=${SSLCOMMERZ_CANCEL_URL:https://ekhoni-server.onrender.com/api/private/payment/fail}

# SMTP email delivery configuration for production OTP and notification flows
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:your-email@example.com}
spring.mail.password=${SMTP_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

################################################################################
## LOYALTY / REWARD POINTS CONFIGURATION
loyalty.points-per-hundred-spend=${LOYALTY_POINTS_PER_HUNDRED_SPEND:1}
loyalty.currency-per-point=${LOYALTY_CURRENCY_PER_POINT:1.0}
loyalty.min-points-to-redeem=${LOYALTY_MIN_POINTS_TO_REDEEM:10}
loyalty.max-discount-percentage=${LOYALTY_MAX_DISCOUNT_PERCENTAGE:50}
loyalty.expiry-days=${LOYALTY_EXPIRY_DAYS:365}

################################################################################
## REFERRAL PROGRAM CONFIGURATION
referral.referrer-bonus-points=${REFERRAL_REFERRER_BONUS:50}
referral.referee-bonus-points=${REFERRAL_REFEREE_BONUS:30}
referral.share-base-url=${REFERRAL_SHARE_BASE_URL:efood://referral?code=}
referral.max-referrals-per-day=${REFERRAL_MAX_PER_DAY:10}
